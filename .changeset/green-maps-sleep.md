---
'@ankhorage/templates': minor
---

Emit runtime `dataSources` and `dataBindings` for the nutrition catalog scan starter so scanner lookups and product-card navigation no longer depend on opaque action strings.
